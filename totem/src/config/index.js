const CONFIG = { url: 'http://localhost:3000/', url2: 'http://localhost:9000' }
const updateInterval = 5000
const totemId = 1
const drinkFieldName = '{GASEOSA}'

export { CONFIG, totemId, updateInterval, drinkFieldName }
