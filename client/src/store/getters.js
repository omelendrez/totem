export const fetchCategories = (state) => {
  return state.categories;
};

export const fetchProducts = (state) => {
  return state.products;
};

export const fetchBasket = (state) => {
  return state.basket;
};
